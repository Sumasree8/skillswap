# ─── SkillSwap Frontend — Environment Variables ──────────────────────────────
# Copy to .env and fill in values.

# Leave blank in development (Vite proxy handles /api → localhost:5000).
# Set to your backend URL in production, e.g. https://api.myapp.com
VITE_API_URL=
